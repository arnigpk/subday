-- ============================================================================
-- POS-статус заказа: отражаем реальное состояние заказа на стороне кассы.
--   pos_status: NULL (не проверяли) | 'open' | 'closed' | 'cancelled'.
-- Когда заказ УЖЕ закрыт (оплачен) на кассе — в кабинете партнёра он показывается
-- закрытым, а кнопка «Отменить» скрывается (отменять уже нечего/некорректно;
-- при необходимости — только на самой кассе).
-- Проверка статуса поддержана всеми POS: Poster (dash.getTransaction),
-- iiko (/api/1/deliveries/by_id), Rosta (GET /orders/{id}). У Rosta кнопки
-- отмены нет вовсе — там менять нечего.
-- Чисто дополняющее поле: на создание/ретрай заказов не влияет.
-- ============================================================================

ALTER TABLE public.iiko_order_log ADD COLUMN IF NOT EXISTS pos_status text;
